import React from 'react'

// This is the default 404 page that will be displayed if a user tries to access a non-existent page.
export default class Default extends React.Component {
    render() {
        return(
            <div>
                <h1>Page not found!</h1>
            </div>
        )
    }
}